I also want servers to have the exact same navbar as the owners (Dashboard, Stock, Finance, Clients, Team, Tables). In 'Stock', they will see the entire stock. In 'Finance', they will see only their own personal finances, and I want it to look just like the owners' interface. In 'Clients', the same logic applies: they should only see the clients they have personally served, using the same layout as the owners. In 'Team', they should see the whole team displayed in a hierarchy and by top performances. In 'Tables', they will see all the tables. I want everything to look exactly like the owners' interfaces.
